meta {
  name: create users
  type: http
  seq: 3
}

post {
  url: http://localhost:3001/api/users
  body: json
  auth: inherit
}

body:json {
  {
    "role": "user"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
